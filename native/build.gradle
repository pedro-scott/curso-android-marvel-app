import com.example.buildsrc.Environment

plugins {
    id 'com.android.library'
    id 'kotlin-android'
}

android {
    compileSdkVersion Environment.Sdk.compileSdk

    externalNativeBuild {
        cmake {
            path "CMakeLists.txt"
        }
    }
}